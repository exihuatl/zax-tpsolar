const { colors } = require("tailwindcss/defaultTheme");

module.exports = {
  theme: {
    container: {
      center: true,
    },
    colors: {
      ...colors,
      primary: "#EF7F1A",
    },
    extend: {},
  },
  variants: {},
  plugins: [],
};
